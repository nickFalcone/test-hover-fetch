{
  "name": "test-hover-fetch",
  "version": "1.0.0",
  "scripts": {
    "dev": "browserify app.js -o bundle.js && http-server",
    "test": "jest",
    "deploy": "browserify app.js -o bundle.js && surge --domain hover-fetch.surge.sh"
  },
  "dependencies": {
    "@nfalcone/hover-fetch": "^0.2.0-alpha"
  },
  "devDependencies": {
    "eslint": "^7.18.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.22.1",
    "http-server": "^0.12.3",
    "jest": "^26.6.3",
    "jest-puppeteer": "^4.4.0",
    "jsdom": "^16.4.0",
    "puppeteer": "^7.0.4",
    "surge": "^0.21.7"
  }
}
