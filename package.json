{
  "name": "test-hover-fetch",
  "version": "1.0.0",
  "scripts": {
    "dev": "browserify app.js -o bundle.js && http-server",
    "deploy": "browserify app.js -o bundle.js && surge"
  },
  "dependencies": {
    "@nfalcone/hover-fetch": "0.1.0-alpha"
  },
  "devDependencies": {
    "eslint": "^7.18.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.22.1",
    "http-server": "^0.12.3",
    "surge": "^0.21.7"
  }
}
